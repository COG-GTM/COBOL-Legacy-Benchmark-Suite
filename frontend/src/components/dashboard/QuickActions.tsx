import { useNavigate } from 'react-router-dom';
import { Briefcase, Clock, BarChart3 } from 'lucide-react';

const actions = [
  {
    title: 'Portfolio Position Inquiry',
    description:
      'Look up portfolio positions by account number. View fund details, units held, cost basis, and current market value.',
    icon: Briefcase,
    to: '/portfolio-inquiry',
    color: 'text-blue-600',
    bg: 'bg-blue-50',
  },
  {
    title: 'Transaction History',
    description:
      'View transaction history for an account. Browse buy, sell, transfer, and fee transactions with date filtering.',
    icon: Clock,
    to: '/transaction-history',
    color: 'text-indigo-600',
    bg: 'bg-indigo-50',
  },
  {
    title: 'Reports',
    description:
      'View position, audit, and statistics reports generated by the daily batch pipeline.',
    icon: BarChart3,
    to: '/reports',
    color: 'text-emerald-600',
    bg: 'bg-emerald-50',
  },
];

export default function QuickActions() {
  const navigate = useNavigate();

  return (
    <section>
      <h2 className="text-lg font-semibold text-gray-900 mb-4">Quick Actions</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {actions.map((action) => (
          <button
            key={action.to}
            onClick={() => navigate(action.to)}
            className="bg-white rounded-lg shadow-sm border border-gray-200 p-6 text-left hover:shadow-md transition-shadow cursor-pointer group"
          >
            <div className={`inline-flex items-center justify-center h-10 w-10 rounded-lg ${action.bg} mb-4`}>
              <action.icon className={`h-5 w-5 ${action.color}`} />
            </div>
            <h3 className="text-base font-semibold text-gray-900 mb-2 group-hover:text-blue-600 transition-colors">
              {action.title}
            </h3>
            <p className="text-sm text-gray-500 leading-relaxed">{action.description}</p>
          </button>
        ))}
      </div>
    </section>
  );
}
