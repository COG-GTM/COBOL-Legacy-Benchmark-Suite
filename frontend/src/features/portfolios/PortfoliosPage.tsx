import { PlaceholderPage } from '../../components/PlaceholderPage';

export function PortfoliosPage() {
  return (
    <PlaceholderPage
      title="Portfolios"
      description="Manage portfolio master records (create, view, update, close)."
      plannedIn="MBA-1426 — Portfolio Management (CRUD)"
    />
  );
}
