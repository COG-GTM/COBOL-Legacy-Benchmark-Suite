import { PageLayout } from '@/components/shared';

export function BatchStatus() {
  return (
    <PageLayout title="Batch Job Status" subtitle="Monitor batch processing status">
      <div className="bg-white rounded-lg border border-border p-8 text-center text-secondary">
        Batch Job Status — implementation pending
      </div>
    </PageLayout>
  );
}
