import { PlaceholderPage } from '@/pages/PlaceholderPage';

export function BatchMonitorPage() {
  return <PlaceholderPage title="Batch Monitor" description="Monitor batch job execution and status" />;
}
