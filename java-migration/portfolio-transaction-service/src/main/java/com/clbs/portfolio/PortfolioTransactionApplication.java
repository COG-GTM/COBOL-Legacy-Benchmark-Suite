package com.clbs.portfolio;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot entry point for the modernized PORTTRAN
 * (Portfolio Transaction Processing) service.
 */
@SpringBootApplication
public class PortfolioTransactionApplication {

    public static void main(String[] args) {
        SpringApplication.run(PortfolioTransactionApplication.class, args);
    }
}
