/**
 * Batch processing layer, mirroring {@code src/programs/batch/} (BCHCTL00, POSUPDT,
 * HISTLD00, report writers, checkpoint/restart). JCL job streams will be re-implemented
 * with Spring Batch rather than translated line-for-line.
 */
package com.ipms.batch;
