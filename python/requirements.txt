# Core dependencies
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# API layer (replaces CICS)
fastapi>=0.100,<1.0
uvicorn>=0.23,<1.0
pydantic>=2.0,<3.0

# Batch orchestration (replaces z/OS JCL scheduler)
prefect>=2.0,<3.0

# Error retry logic (replaces DB2ERR retry loop)
tenacity>=8.0,<9.0

# Reporting (replaces 132-byte fixed-width COBOL reports)
pandas>=2.0,<3.0
jinja2>=3.1,<4.0

# Authentication (replaces RACF/CICS security)
PyJWT>=2.8,<3.0

# Test data generation (replaces TSTGEN00)
faker>=20.0,<30.0

# Development / testing
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0
ruff>=0.1,<1.0
