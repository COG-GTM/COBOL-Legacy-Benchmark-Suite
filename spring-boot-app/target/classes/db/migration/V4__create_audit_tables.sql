-- =====================================================================
-- V4: AuditRecord and ErrorLog tables
-- Migrated from COBOL copybooks: AUDITLOG.cpy, DBTBLS.cpy (ERRLOG)
-- and DB2 SQL: ERRLOG.sql
-- =====================================================================

CREATE TABLE audit_record (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    timestamp       TIMESTAMP       NOT NULL,
    system_id       VARCHAR(8),
    user_id         VARCHAR(8),
    program         VARCHAR(8),
    terminal        VARCHAR(8),
    audit_type      VARCHAR(20)     NOT NULL,
    action          VARCHAR(10)     NOT NULL,
    status          VARCHAR(10)     NOT NULL,
    portfolio_id    VARCHAR(8),
    account_no      VARCHAR(10),
    before_image    VARCHAR(100),
    after_image     VARCHAR(100),
    message         VARCHAR(100)
);

CREATE TABLE error_log (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    error_timestamp TIMESTAMP       NOT NULL,
    program_id      VARCHAR(8)      NOT NULL,
    error_type      VARCHAR(15)     NOT NULL,
    error_severity  VARCHAR(10)     NOT NULL,
    error_code      VARCHAR(8)      NOT NULL,
    error_message   VARCHAR(200)    NOT NULL,
    process_date    DATE            NOT NULL,
    process_time    TIME            NOT NULL,
    user_id         VARCHAR(8)      NOT NULL,
    additional_info VARCHAR(500)
);
