import React from 'react';
import ReactDOM from 'react-dom/client';
import BatchProcessInitialize from './BatchProcessInitialize';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BatchProcessInitialize />
  </React.StrictMode>
);
