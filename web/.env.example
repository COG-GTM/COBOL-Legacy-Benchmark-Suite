# Session inactivity timeout in milliseconds (default: 900000 = 15 minutes)
VITE_SESSION_TIMEOUT_MS=900000

# How long before the timeout to show the warning dialog, in milliseconds
# (default: 60000 = 1 minute)
VITE_SESSION_WARNING_MS=60000
