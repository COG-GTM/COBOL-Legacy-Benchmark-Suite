# Dev-server proxy target for /api (used by vite.config.ts during `npm run dev`)
VITE_API_TARGET=http://localhost:4000

# Optional: absolute API base URL baked in at build time (production).
# When unset, the app calls same-origin relative URLs ("/api/...").
# VITE_API_BASE_URL=https://api.example.com
